<style>
    :root {
        --gl-bg: #0b1020;
        --gl-card-bg: #0b1220;
        --gl-text: #e5e7eb;
        --gl-sub: #9ca3af;
        --gl-border: #1f2937;
        --gl-accent: #60a5fa;
        --gl-shadow: 0 10px 25px rgba(0,0,0,.45);
    }
    html, body { height: 100%; }
    body { margin: 0; font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, Helvetica Neue, Arial, "Apple Color Emoji", "Segoe UI Emoji"; color: var(--gl-text); background: var(--gl-bg); }
    .gl-wrap { min-height: 100vh; padding: 4rem 1rem; display: grid; place-items: center; background: radial-gradient(1200px 500px at 50% -10%, var(--gl-bg), transparent 70%); }
    .gl-card { width: 100%; max-width: 720px; background: var(--gl-card-bg); color: var(--gl-text); border: 1px solid var(--gl-border); border-radius: 16px; box-shadow: var(--gl-shadow); padding: 2rem; }
    .gl-icon { font-size: 2.25rem; line-height: 1; }
    .gl-title { margin: .5rem 0 0; font-size: clamp(1.4rem, 2.5vw, 1.8rem); }
    .gl-lead { color: var(--gl-sub); margin: .5rem 0 1.25rem; }
    .gl-context { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: .75rem; margin: 1rem 0 1.5rem; }
    .gl-kv { padding: .75rem 1rem; border: 1px dashed var(--gl-border); border-radius: 10px; }
    .gl-kv span { display: block; font-size: .8rem; color: var(--gl-sub); margin-bottom: .25rem; }
    .gl-actions { display: flex; flex-wrap: wrap; gap: .75rem; margin-top: .25rem; }
    .gl-btn { display: inline-flex; align-items: center; justify-content: center; padding: .625rem 1rem; border-radius: 10px; text-decoration: none; font-weight: 600; border: 1px solid transparent; }
    .gl-btn.gl-primary { background: var(--gl-accent); color: #fff; }
    .gl-btn.gl-secondary { background: transparent; color: var(--gl-text); border-color: var(--gl-border); }
    .gl-help { color: var(--gl-sub); font-size: .9rem; margin-top: 1rem; }
</style>
